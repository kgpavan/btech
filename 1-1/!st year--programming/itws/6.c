//an array A and size n is called " circular sorted array with index i" if the following sequence of elements are in sorted order........
//A[i],A[i+1],A[i+2],..........A[n],A[0],A[1].......A[i-1]
//Given A and a value v,write a prog. to search for v in A using binary search without sorting A or using any extra array .
#include<stdio.h>
main()
{
	int n;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	int input;
	scanf("%d",&input);
	lower=a[0];
	for(i=0;i<n;
